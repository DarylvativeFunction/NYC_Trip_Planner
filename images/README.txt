Drop the two stop photos here with these EXACT names:

  charging-bull.jpg   → the Charging Bull statue photo
  liberty-bagels.jpg  → the Liberty Bagels Midtown storefront photo

The Places tab loads them from this folder. Until they're added, those two
cards show their emoji header instead (no error). JPG or PNG is fine — if you
use PNG, change the extension in the code (PLACE_MEDIA) to match.
